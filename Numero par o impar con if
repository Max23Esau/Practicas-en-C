#include <stdio.h>

int main() {
 int numero;
 
 printf("Ingrese un numero: ");
 scanf("%i", &numero);
 
 if(numero % 2 == 0)
 {
   printf("El numero %i es par", numero);
 }
 else
 {
   printf("El numero %i es impar", numero);
 }
 return 0;
}
